# .env.template
# Hermes Persistence Stack - Replication Pack

# Database Configuration
DB_USER=gentleman
DB_PASSWORD=hermes_secret_2026
DB_NAME=engram
DB_HOST=localhost
DB_PORT=5432

# Engram & Hermes Bridge
# This URL is used by Engram to connect to the PostgreSQL instance
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Version Control
GENTLE_VERSION=1.26.5
ENGRAM_VERSION=1.15.10
HERMES_VERSION=0.13.0

# General Settings
DEBUG=false
LOG_LEVEL=INFO

# Tooling & MCP (WordPress)
WP_SITES_PATH=./wp-sites.json
