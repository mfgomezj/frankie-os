# .env.template
# Hermes Persistence Stack - Replication Pack

# === Database Configuration ===
DB_USER=gentleman
DB_PASSWORD=hermes_secret_2026
DB_NAME=engram
DB_HOST=localhost
DB_PORT=5432

# Engram & Hermes Bridge
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# === API Credentials (Frankie Cloud) ===
OPENROUTER_API_KEY=sk-or-v1-REEMPLAZAR
TELEGRAM_BOT_TOKEN=REEMPLAZAR
TELEGRAM_ALLOWED_USERS=REEMPLAZAR
TELEGRAM_HOME_CHANNEL=REEMPLAZAR
TELEGRAM_HOME_CHANNEL_NAME=Frankie_Cloud_Home

NOTION_ACCESS_TOKEN=ntn_REEMPLAZAR
N8N_URL=https://engine.REEMPLAZAR.online
N8N_API_KEY=REEMPLAZAR

# === AI Configuration ===
DEFAULT_MODEL=nvidia/nemotron-3-super-120b-a12b:free
LLM_PROVIDER=openrouter

# === Versions ===
GENTLE_VERSION=1.28.2
ENGRAM_VERSION=1.15.10
HERMES_VERSION=0.13.0

# === General Settings ===
DEBUG=false
LOG_LEVEL=INFO

# === Tooling & MCP ===
WP_SITES_PATH=./wp-sites.json
ASSETS_BANK_ROOT=D:/BANK
